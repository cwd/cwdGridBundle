<input class="form-control filter filter-split-fields" data-field="{{ column.name }}" data-operator="gteq" type="date" style="width:45%; float:left; font-size:80%" name="filters[]" value="{{ value.from|escape }}" />
<input class="form-control filter filter-split-fields" data-field="{{ column.name }}" data-operator="lteq" type="date" style="width:45%; float:right; font-size:80%" name="filters[]" value="{{ value.to|escape }}" />